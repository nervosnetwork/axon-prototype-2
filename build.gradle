/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/6.9/samples
 */


buildscript {

    repositories {
        jcenter()
    }

    dependencies {

    }
}

plugins {
    id 'java'
}

allprojects {

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    group 'org.nervos.axon'
    version '0.1.0'
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
    }

    test {
    }

}

subprojects {
}

task dockerBuild(){
    dependsOn('docker')
}

task compileCkbContracts (type: Exec){
    setWorkingDir project.file('axon-ckb-contracts')
    commandLine 'capsule', 'build','--' ,'--features', 'real-deploy'
}